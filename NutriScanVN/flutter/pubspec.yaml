name: nutriscan_vn
description: NutriScanVN - AI Calorie Scanner & Nutrition Tracker
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  image_picker: ^1.1.2
  camera: ^0.11.0+2
  shared_preferences: ^2.3.2
  intl: ^0.19.0

  # UI/Charts
  fl_chart: ^0.69.0
  lottie: ^3.1.2

  # 3D / nice UI (placeholder package refs)
  rive: ^0.13.14

  # For JWT decode if needed
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.12
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/lottie/
